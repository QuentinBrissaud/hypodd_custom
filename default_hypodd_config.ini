[relocator]
# Default wrapper behavior uses cross-correlation and catalog differential
# times together (HypoDD IDATA=3).
use_cross_correlation = true

# No fixed-parameter flag is added to event.sel by default.
event_fix =

# Catalog depths are used by default.
fixed_depth_km =

[ph2dt]
# Default ph2dt parameters used by hypoddpy.
MINWGHT = 0.0

# MAXDIST is computed automatically to include all event-station pairs.
# MAXDIST =

# MAXSEP is computed automatically from the event geometry.
# MAXSEP =

MAXNGH = 10
MINLNK = 8
MINOBS = 8
MAXOBS = 50

[hypodd]
# Use P and S phases.
IPHA = 3

# Default for use_cross_correlation=true. If use_cross_correlation=false, the
# wrapper defaults become OBSCC=0 and OBSCT=8.
OBSCC = 8
OBSCT = 0

# Disable distance/gap filters inside HypoDD.
MINDS = -999
MAXDS = -999
MAXGAP = -999

# Start from catalog locations, use LSQR, remove air quakes.
ISTART = 2
ISOLV = 2
IAQ = 2

# Relocate all clusters and all events.
CID = 0
ID =

# Default cross-correlation plus catalog-data iteration rows:
# NITER WTCCP WTCCS MAXRCC MAXDCC WTCTP WTCTS MAXRCT MAXDCT DAMP
ITERATIONS =
    100 1 0.5 -999 -999 0.1 0.05 -999 -999 30
    100 1 0.5 6 -999 0.1 0.05 6 -999 30

[compiler]
# Defaults used by the wrapper when compiling HypoDD. MAXEVE and MAXSTA are
# computed automatically from the number of events/stations.
MAXEVE0 = 200
MAXDATA = 3000000
MAXDATA0 = 60000
MAXCL = 20
